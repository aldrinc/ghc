"""Testimonial renderer ported from the `testimonial-renderer` service.

This package provides:
- strict payload validation (ported from src/lib/validate.mjs)
- Playwright-based HTML template rendering to PNG
"""


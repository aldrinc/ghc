# Root-Cause Diagnosis

Root cause: the workflow mixes three concerns that mOS must separate:

- creative production
- provider publication
- conversion feedback and decisioning

Larry runs them as scripts over local files. mOS needs durable entities, adapters, approvals, sync runs, source-backed metrics, and Hermes tools.

The right abstraction is a content growth program with pluggable publishing and conversion adapters.

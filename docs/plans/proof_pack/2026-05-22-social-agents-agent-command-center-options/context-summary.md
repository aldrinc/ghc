# Context Summary

The current `SocialAgentsPage` implementation is a six-step wizard with the primary labels `Choose Program`, `Add Conversion Source`, `Create Experiment`, `Write Carousel`, `Render Media`, and `Postiz Handoff`.

Live browser observation confirmed those backend-shaped labels are visible as the primary workflow. The new plan reframes the same backend logic as customer-facing agent setup and management.

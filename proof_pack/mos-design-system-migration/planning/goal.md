# Goal Artifact

Migrate `mos/frontend` to the new MOS product design system while preserving all brand identity elements.

Key results:

- Canonical tokens drive Tailwind and CSS for product UI.
- Core UI primitives match the supplied design-system reference.
- Representative product routes render consistently on desktop and mobile.
- Brand identity files and blocks remain unchanged unless explicitly approved.
- Local tests/build/visual smoke checks pass during implementation.

Timeframe:

- One approved implementation pass after the user says `Ship the plan`.

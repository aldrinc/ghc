# Current Machine

- Floating panel shell comes from `floatingPanel`.
- `DialogContent` and `AlertDialogContent` set width and padding.
- Title primitives set display font and size.
- Description primitives set muted color and small text.
- Header spacing is left to each caller.
- Component review examples manually add margin, masking the default issue.


# Problem Log

1. Modal title and description are visually cramped in the delete workspace confirmation.
2. Shared `DialogDescription` and `AlertDialogDescription` have no default top margin or tuned line-height.
3. Some call sites manually add `mt-1` or `mt-2`, so readable spacing depends on caller memory.
4. Component review examples hide the primitive weakness by adding manual spacing.


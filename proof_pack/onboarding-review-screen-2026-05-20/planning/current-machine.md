# Current Machine

The user reaches a review step, sees a change panel, then sees the full editable form. The user must mentally convert form controls into a read-only summary before deciding whether to create the workspace.

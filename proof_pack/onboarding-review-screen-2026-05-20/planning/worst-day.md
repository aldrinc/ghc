# Worst-Day Test

If extraction returns missing pricing and long offer text, the review screen still keeps missing pricing visible near the top, truncates long details into readable rows, and allows workspace creation when required setup fields are complete.

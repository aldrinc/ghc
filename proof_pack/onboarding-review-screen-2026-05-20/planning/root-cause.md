# Root Cause Diagnosis

Root cause: the review step has no separate review model. It reuses the edit form as the confirmation UI.

System flaw: extracted/manual data, attention states, and edit controls are not separated into review mode and edit mode.

# Problem Log

Primary problem: review and editing are mixed on one screen.

Observable symptoms:

- The review step renders inputs, selects, and textareas by default.
- Missing pricing appears in the same visual weight as normal editable fields.
- Long text fields dominate the first screen and slow review.
